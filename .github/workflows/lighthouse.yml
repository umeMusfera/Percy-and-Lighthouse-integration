name: Lighthouse CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      # Optional: If you have a build command
      - name: Build project
        run: npm run build

      # Run Lighthouse CI
      - name: Run Lighthouse CI
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ 66ir4gNKZOQ:94469816:OwmWTnze2kFEj}}

# # 66ir4gNKZOQ:94469816:OwmWTnze2kFEj